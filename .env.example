GEMINI_API_KEY=your-free-gemini-api-key-here
# Optional: a key from a SEPARATE Google AI Studio project, used only by the
# backtest so it never spends the live app's free-tier quota.
GEMINI_BACKTEST_API_KEY=
# Optional: a key from a SEPARATE Google AI Studio project, used only by the
# backtest so it never spends the live app's free-tier quota.
GEMINI_BACKTEST_API_KEY=
;
cat
.env.example;
grep
-c
def use_api_key
services/gemini_client.py
-s
true
-e
0
